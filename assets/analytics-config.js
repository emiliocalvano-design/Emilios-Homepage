// Set this to your deployed Worker URL after `wrangler deploy`.
// e.g. "https://emilio-analytics.YOURNAME.workers.dev"
// Both the page beacon (track.js) and the dashboard (console.html) read it.
window.ANALYTICS_API = "https://REPLACE-WITH-YOUR-WORKER.workers.dev";
